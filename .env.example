# Shopify Configuration
# Replace with your actual shop domain (without .myshopify.com)
SHOPIFY_SHOP_DOMAIN=your-shop-name

# Replace with your Shopify private app access token
SHOPIFY_ACCESS_TOKEN=your-shopify-access-token

# Optional: Shopify API version (defaults to 2023-10)
SHOPIFY_API_VERSION=2023-10

# Optional: Request timeout in milliseconds (defaults to 2000)
SHOPIFY_TIMEOUT=2000

# Optional: Number of retries on failure (defaults to 1)
SHOPIFY_RETRIES=1