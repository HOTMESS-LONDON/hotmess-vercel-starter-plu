export default function Footer() {
  return (
    <footer style={{padding:'24px 16px',borderTop:'1px solid #222',marginTop:48,opacity:.8}}>
      <small>© HOTMESS London — Always too much, never enough.</small>
    </footer>
  );
}
